import { sanity } from '~/lib/sanity'

const posts = await sanity.fetch(`*[_type == "post"] | order(publishedAt desc) {
  title,
  slug,
  mainImage {
    asset->{
      url
    }
  },
  publishedAt
}`)

